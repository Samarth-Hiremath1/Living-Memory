# Template only — DO NOT commit real values.
#
# Create the real secret out-of-band:
#
#   kubectl create secret generic living-memory-secrets \
#     --from-literal=anthropic-api-key="$ANTHROPIC_API_KEY" \
#     --from-literal=elevenlabs-api-key="$ELEVENLABS_API_KEY" \
#     --from-literal=aviationstack-api-key="$AVIATIONSTACK_API_KEY"
#
# In a real cluster these would come from External Secrets Operator backed by
# AWS Secrets Manager or Vault, not from a literal.

apiVersion: v1
kind: Secret
metadata:
  name: living-memory-secrets
type: Opaque
stringData:
  anthropic-api-key: "REPLACE_ME"
  elevenlabs-api-key: ""
  aviationstack-api-key: ""
